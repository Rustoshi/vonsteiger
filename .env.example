# Site URL (used for SEO canonical URLs, sitemap, OG tags)
NEXT_PUBLIC_SITE_URL=https://www.vonsteiger.law

# MongoDB Connection
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/vonsteiger?retryWrites=true&w=majority

# ZeptoMail (Zoho) Transactional Email
ZEPTOMAIL_API_KEY=your_zeptomail_api_key_here
ZEPTOMAIL_FROM_EMAIL=noreply@vonsteiger.law

# Contact notification recipient
CONTACT_NOTIFICATION_EMAIL=contact@vonsteiger.law

# JWT Secret for admin authentication (generate a random 64-char string)
JWT_SECRET=your-jwt-secret-change-me-in-production

# Admin seed credentials (used by scripts/seed-admin.ts)
ADMIN_EMAIL=admin@vonsteiger.law
ADMIN_PASSWORD=VonSteiger2026!
ADMIN_NAME=Julian von Steiger
